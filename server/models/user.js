// Generated by CoffeeScript 1.6.3
var PermissionSchema, RoleSchema, Schema, UserSchema, crypto, mongoose, resolvePermission;

mongoose = require('mongoose');

crypto = require('crypto');

Schema = mongoose.Schema;

PermissionSchema = new Schema({
  subject: {
    type: String,
    "default": ''
  },
  action: {
    type: String,
    "default": ''
  },
  displayName: String,
  description: String
});

mongoose.model('Permission', PermissionSchema);

resolvePermission = function(permission, done) {
  mongoose.model('Permission').findOne({
    subject: permission.subject,
    action: permission.action
  }, function(err, permission) {
    if (err) {
      return done(err);
    }
    if (!permission) {
      return done(new Error("Unknown Permission"));
    }
    done(null, permission);
  });
};

RoleSchema = new Schema({
  name: {
    type: String,
    "default": ''
  },
  displayName: String,
  description: String,
  permissions: [PermissionSchema]
});

RoleSchema.methods = {
  hasPermission: function(subject, action) {
    var p, _i, _len, _ref;
    if (this.permissions) {
      _ref = this.permissions;
      for (_i = 0, _len = _ref.length; _i < _len; _i++) {
        p = _ref[_i];
        if (subject === p.subject && action === p.action) {
          return true;
        }
      }
    }
    return false;
  },
  addPermission: function(permission, done) {
    resolvePermission(permission, function(err, permission) {
      if (err) {
        return done(err);
      }
      this.permissions.push(permission._id);
      return this.save(done);
    });
  }
};

mongoose.model('Role', RoleSchema);

UserSchema = new Schema({
  username: {
    type: String,
    "default": ''
  },
  hashed_password: {
    type: String,
    "default": ''
  },
  email: {
    type: String,
    "default": ''
  },
  authToken: {
    type: String,
    "default": ''
  },
  salt: {
    type: String,
    "default": ''
  },
  roles: [RoleSchema]
});

UserSchema.virtual('password').set(function(password) {
  this._password = password;
  this.salt = this.makeSalt();
  return this.hashed_password = this.encryptPassword(password);
}).get(function() {
  return this._password;
});

UserSchema.methods = {
  authenticate: function(plainText) {
    return this.encryptPassword(plainText) === this.hashed_password;
  },
  validateToken: function(token, tokenDecoded) {
    var now;
    if (token !== this.authToken) {
      return false;
    }
    now = Math.round(new Date().getTime() / 1000);
    return tokenDecoded.expires > now;
  },
  makeSalt: function() {
    return Math.round(new Date().valueOf() * Math.random()) + '';
  },
  encryptPassword: function(password) {
    var encrypred, err;
    if (!password) {
      return '';
    }
    try {
      encrypred = crypto.createHmac('sha1', this.salt).update(password).digest('hex');
      return encrypred;
    } catch (_error) {
      err = _error;
      return '';
    }
  }
};

mongoose.model('User', UserSchema);
